{
  "libpg conninfo": "host=localhost dbname=intelmq user=intelmq password=intelmq",
  "logging_level": "INFO",
  "subqueries": {
    "feed-code_is": {
      "sql": "\"feed.code\" = %s",
      "description": "Check if the feed.code fields equals to the given value.",
      "label": "feed.code equals to",
      "ext_type": "string"
    },
    "source-abuse_contact_is": {
      "sql": "\"source.abuse_contact\" = %s",
      "description": "Check if the source.abuse_contact fields equals to the given value.",
      "label": "source.abuse_contact equals to",
      "ext_type": "string"
    },
    "malware-has-md5_is": {
      "sql": "\"malware.hash.md5\" = %s",
      "description": "Check if the malware.hash.md5 fields equals to the given value.",
      "label": "malware.hash.md5 equals to",
      "ext_type": "string"
    }
  }
}
